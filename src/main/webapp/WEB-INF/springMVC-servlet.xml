<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:p="http://www.springframework.org/schema/p" xmlns:tx="http://www.springframework.org/schema/tx"
	xmlns:aop="http://www.springframework.org/schema/aop" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xsi:schemaLocation="
       http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans-3.2.xsd
       http://www.springframework.org/schema/mvc
       http://www.springframework.org/schema/mvc/spring-mvc-3.2.xsd
       http://www.springframework.org/schema/context
       http://www.springframework.org/schema/context/spring-context-3.2.xsd">

	<!-- 启动注解驱动的Spring MVC,请求url的注册和POJO类方法映射的注解 -->
	<mvc:annotation-driven />
	<!-- <bean class="org.springframework.web.servlet.mvc.annotation.DefaultAnnotationHandlerMapping" 
		/> <bean class="org.springframework.web.servlet.mvc.annotation.AnnotationMethodHandlerAdapter" 
		/> -->

	<!-- 自动扫描以下包作为MVC组件,@Controller、@Service、@repository、@Component等注解标识 -->
	<context:component-scan base-package="com.BeAnArtist.FirstStep.controller" />
	<context:component-scan base-package="com.BeAnArtist.FirstStep.dao" />
	<context:component-scan base-package="com.BeAnArtist.FirstStep.service" />
	<context:component-scan base-package="com.BeAnArtist.FirstStep.command" />

	<!-- 视图解析 -->
	<bean class="org.springframework.web.servlet.view.UrlBasedViewResolver">
		<!-- jstl处理返回的视图模型数据 -->
		<property name="viewClass"
			value="org.springframework.web.servlet.view.JstlView" />
		<!-- 视图页面的前缀和后缀,若传进来的逻辑视图名为index，则对应视图:/WEB-INF/views/index.jsp -->
		<property name="prefix" value="/WEB-INF/views/" />
		<property name="suffix" value=".jsp" />
	</bean>

	<!-- 对静态资源文件的访问使用默认servlet处理,WEB-INF目录禁止访问 -->
	<mvc:default-servlet-handler />

	<!-- 字符转码UTF-8 -->
	<bean id="stringConverter"
		class="org.springframework.http.converter.StringHttpMessageConverter">
		<property name="supportedMediaTypes">
			<list>
				<value>text/plain;charset=UTF-8</value>
				<value>text/html;charset=UTF-8</value>
			</list>
		</property>
	</bean>

	<!-- 输出对象转JSON支持 -->
	<bean id="jsonConverter"
		class="org.springframework.http.converter.json.MappingJackson2HttpMessageConverter" />
</beans>